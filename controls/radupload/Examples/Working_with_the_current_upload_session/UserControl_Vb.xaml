<UserControl x:Class="Default_Vb"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
            
    
<!-- #region Working_with_the_current_upload_session_0 -->
private void RadUpload_UploadFinished(object sender, EventArgs e)
{
RadUpload upload1 = sender as RadUpload;
//Current Progress: upload1.CurrentSession.CurrentProgress.ToString()
//Current File: upload1.CurrentSession.CurrentFile.Name
//Current FileProgress: upload1.CurrentSession.CurrentFileProgress
//TotalFilesCount: upload1.CurrentSession.TotalFilesCount
//TotalFileSize: upload1.CurrentSession.TotalFileSize
//UploadedBytes: upload1.CurrentSession.UploadedBytes
//UploadedFilesCount: upload1.CurrentSession.UploadedFiles.Count
Collection<RadUploadSelectedFile> uploadedFiles = upload1.CurrentSession.UploadedFiles;
}
<!-- #endregion -->
</Grid>
</UserControl>
